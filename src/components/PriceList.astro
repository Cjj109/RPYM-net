---
import type { Category, BCVRate } from '../lib/sheets';
import ProductCard from './ProductCard.astro';

interface Props {
  categories: Category[];
  bcvRate: BCVRate;
}

const { categories, bcvRate } = Astro.props;

// Iconos por categoría
const categoryIcons: Record<string, string> = {
  'Camarones': '🦐',
  'Calamares': '🦑',
  'Mariscos': '🦀',
  'Especiales': '👑',
};
---

<section id="precios" class="py-10 md:py-16 px-4">
  <div class="container mx-auto max-w-6xl">
    <div class="text-center mb-6 md:mb-8">
      <h2 class="text-2xl md:text-3xl lg:text-4xl font-display font-bold text-ocean-900 mb-3 md:mb-4">
        Lista de Precios
      </h2>
      <p class="text-ocean-700 max-w-2xl mx-auto text-sm md:text-base">
        Productos frescos del día, directamente del muelle a tu mesa.
        Los precios pueden variar según disponibilidad.
      </p>
    </div>

    <!-- Indicador de tasa BCV - Compacto en móvil -->
    <div class="flex justify-center mb-6 md:mb-10">
      <div class="inline-flex items-center gap-2 md:gap-3 bg-white border border-ocean-200 rounded-full px-3 py-2 md:px-5 md:py-2.5 shadow-sm">
        <div class="flex items-center gap-1.5 md:gap-2">
          <span class="w-2 h-2 bg-green-500 rounded-full animate-pulse"></span>
          <span class="text-xs md:text-sm font-medium text-ocean-700">Tasa BCV</span>
        </div>
        <div class="h-4 w-px bg-ocean-200"></div>
        <span class="text-base md:text-lg font-bold text-ocean-900">
          Bs. {bcvRate.rate.toFixed(2)}
        </span>
        <span class="text-[10px] md:text-xs text-ocean-700">
          / USD
        </span>
      </div>
    </div>

    <div class="space-y-6 md:space-y-10">
      {categories.map((category) => (
        <div class="bg-ocean-50/50 rounded-xl md:rounded-2xl p-4 md:p-6 lg:p-8">
          <h3 class="text-lg md:text-xl lg:text-2xl font-display font-semibold text-ocean-800 mb-4 md:mb-6 flex items-center gap-2 md:gap-3">
            <span class="text-xl md:text-2xl">{categoryIcons[category.name] || '🐠'}</span>
            {category.name}
            <span class="text-xs md:text-sm font-normal text-ocean-700 font-body">
              ({category.products.length})
            </span>
          </h3>

          {/* Micro-guía de tallas - solo para Camarones */}
          {category.name === 'Camarones' && (
            <div class="mb-4 md:mb-6 bg-gradient-to-r from-ocean-100 to-coral-50 rounded-lg p-3 md:p-4 border border-ocean-200">
              <p class="text-sm md:text-base font-medium text-ocean-800 mb-2">
                🦐 ¿No sabes qué camarón elegir?
              </p>
              <div class="flex flex-wrap gap-2 md:gap-3 text-xs md:text-sm">
                <span class="inline-flex items-center gap-1.5 bg-white/80 px-2.5 py-1 rounded-full border border-ocean-200">
                  <span class="font-bold text-ocean-700">61/70</span>
                  <span class="text-ocean-700">Rendidor</span>
                </span>
                <span class="inline-flex items-center gap-1.5 bg-coral-100 px-2.5 py-1 rounded-full border border-coral-200">
                  <span class="font-bold text-coral-600">41/50</span>
                  <span class="text-coral-700">El más usado</span>
                  <span class="text-[10px]">⭐</span>
                </span>
                <span class="inline-flex items-center gap-1.5 bg-white/80 px-2.5 py-1 rounded-full border border-ocean-200">
                  <span class="font-bold text-ocean-700">36/40</span>
                  <span class="text-ocean-700">Grande</span>
                </span>
                <span class="inline-flex items-center gap-1.5 bg-white/80 px-2.5 py-1 rounded-full border border-ocean-200">
                  <span class="font-bold text-ocean-700">31/35</span>
                  <span class="text-ocean-700">Premium</span>
                </span>
              </div>
              <p class="text-[10px] md:text-xs text-ocean-500 mt-2">
                El número indica cuántos camarones vienen por libra. Menor número = camarón más grande.
              </p>
            </div>
          )}

          <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-3 md:gap-4">
            {category.products.map((product) => (
              <ProductCard product={product} />
            ))}
          </div>
        </div>
      ))}
    </div>

    <p class="text-center text-xs md:text-sm text-ocean-700 mt-6 md:mt-8">
      Tasa {bcvRate.source} actualizada: {bcvRate.date}.
      Consulta disponibilidad por WhatsApp.
    </p>
  </div>
</section>
